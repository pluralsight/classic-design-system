import { HTMLPropsFor, ValueOf, classNames } from '@pluralsight/ps-design-system-util'
import { useTheme } from '@pluralsight/ps-design-system-theme'
import React, { forwardRef } from 'react'

import stylesheet from '../css'

interface {{pascalCase name}}Props extends HTMLPropsFor<'div'> {}

const {{pascalCase name}} = forwardRef((props, forwardedRef) => {
  const { className, ...rest } = props
  const themeName = useTheme()

  const ref = React.useRef<HTMLDivElement>(null)
  React.useImperativeHandle(forwardedRef, () => ref.current as HTMLDivElement)

  return (
    <div
      ref={ref}
      {...rest}
      className={classNames('psds-{{name}}', className)}
    />
  )
})
{{pascalCase name}}.displayName = '{{pascalCase name}}'

export default {{pascalCase name}}
