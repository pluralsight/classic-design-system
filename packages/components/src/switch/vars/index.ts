import { keyMirror } from '../../util'

export const sizes = keyMirror('small', 'large')

export const labelAligns = keyMirror('left', 'right')

export const colors = keyMirror('blue', 'green', 'orange')
