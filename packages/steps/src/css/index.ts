import { names as themeNames } from '@pluralsight/ps-design-system-theme'

import * as vars from '../vars'

export default {
  '.psds-steps': {},
  [`.psds-steps.psds-theme--${themeNames.light}`]: {},
}
