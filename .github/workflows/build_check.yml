on:
  pull_request:
    branches:
      - master

jobs:
  packages-build:
    runs-on: ubuntu-latest
    name: 🚀 Packages can successfully build
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: yarn install
      - run: yarn build

  # TODO: Figure out how to test docs build

  # docs-build:
  #   runs-on: ubuntu-latest
  #   name: 🚀 Docs can successfully build
  #   defaults:
  #     run:
  #       working-directory: docs
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-node@v2
  #     - run: yarn install
  #     - run: yarn build:gatsby
