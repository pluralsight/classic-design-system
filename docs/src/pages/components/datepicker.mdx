---
name: Datepicker
route: /components/datepicker
---

import DatePicker from '@pluralsight/ps-design-system-datepicker'

import { Types, Usage } from '../../components/component'

# Date picker

## Examples

### Labels

Primary identification of a date picker comes through the `label`. Supporting text and error messaging is set in the `subLabel`.

```typescript
import DatePicker from '@pluralsight/ps-design-system-datepicker'
import React from 'react'

function Example() {
  return <div className="example-grid--col-2">
    <DatePicker label="Publish date" />
    <DatePicker
      label="Publish date"
      subLabel="When your course will go live"
    />
  </div>
}

export default Example
```

### Appearance

When using the `dark` theme, a `subtle` appearance is available.

```typescript themeName=dark
import DatePicker from '@pluralsight/ps-design-system-datepicker'
import Theme from '@pluralsight/ps-design-system-theme'
import React from 'react'

function Example() {
  return <Theme name={Theme.names.dark}>
    <DatePicker appearance={DatePicker.appearances.subtle} />
  </Theme>
}

export default Example
```

### Disabled

Disabled date pickers are unmodifiable and diminished visually.

```typescript 
import DatePicker from '@pluralsight/ps-design-system-datepicker'
import React from 'react'

function Example() {
  return <DatePicker disabled label="Can't touch this" />
}

export default Example
```

### Error

Error states are engaged with the `error` flag.  Error-related messaging is sent to the `subLabel` prop.
      
```typescript 
import DatePicker from '@pluralsight/ps-design-system-datepicker'
import React from 'react'

function Example() {
  return <DatePicker error label="Publish date" subLabel="Field is required" />
}

export default Example
```


## Usage & Types

<Usage install="npm install @pluralsight/ps-design-system-datepicker"
  import="import Datepicker from '@pluralsight/ps-design-system-datepicker'"
  packageName="datepicker"
  />

<Types.Table>
  <Types.Prop name="appearance" type={<Types.Enum enum={DatePicker.appearances}/>} desc="visual style (from DatePicker.appearances)" default="default" />
  <Types.Prop name="disabled" type="boolean" desc="standard input disable flag" default="false" />
  <Types.Prop name="error" type="boolean" desc="error state flag" default="false" />
  <Types.Prop name="label" type="string" desc="identifying string for input" />
  <Types.Prop name="onSubBlur" type="('mm/dd/yyyy') => void" desc="on subfield blur, returns null if invalid date" />
  <Types.Prop name="onSelect" type="('mm/dd/yyyy') => void" desc="triggered when new valid date selected" />
  <Types.Prop name="subLabel" type="string" desc="supporting text or error messaging" />
  <Types.Prop name="value" type="string" desc="current date as a string (format mm/dd/yyyy)" />
</Types.Table>

