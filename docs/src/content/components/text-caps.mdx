---
name: Caps
route: /components/text-caps
---

import { Caps } from '@pluralsight/ps-design-system-text'

# Caps

<Intro>For displaying uppsecase inline text elements in UI components.</Intro>

<TableOfContents {...props} />

## Examples

```typescript
import { Caps } from '@pluralsight/ps-design-system-text'
import React from 'react'

const Example: React.FC = props => {
  return (
    <React.Fragment>
      <div>
        <Caps size={Caps.sizes.large} color={Caps.colors.primary} strong>
          Caps (large, Primary, strong)
        </Caps>
      </div>
      <div>
        <Caps size={Caps.sizes.large} color={Caps.colors.primary}>
          Caps (large, Primary)
        </Caps>
      </div>
      <div>
        <Caps size={Caps.sizes.large} color={Caps.colors.secondary} strong>
          Caps (large, secondary, strong)
        </Caps>
      </div>
      <div>
        <Caps size={Caps.sizes.large} color={Caps.colors.secondary}>
          Caps (large, secondary)
        </Caps>
      </div>

      <div>
        <Caps size={Caps.sizes.medium} color={Caps.colors.primary} strong>
          Caps (Medium, Primary, strong)
        </Caps>
      </div>
      <div>
        <Caps size={Caps.sizes.medium} color={Caps.colors.primary}>
          Caps (Medium, Primary)
        </Caps>
      </div>
      <div>
        <Caps size={Caps.sizes.medium} color={Caps.colors.secondary} strong>
          Caps (Medium, secondary, strong)
        </Caps>
      </div>
      <div>
        <Caps size={Caps.sizes.medium} color={Caps.colors.secondary}>
          Caps (Medium, secondary)
        </Caps>
      </div>

      <div>
        <Caps size={Caps.sizes.small} color={Caps.colors.primary} strong>
          Caps (Small, Primary, strong)
        </Caps>
      </div>
      <div>
        <Caps size={Caps.sizes.small} color={Caps.colors.primary}>
          Caps (Small, Primary)
        </Caps>
      </div>
      <div>
        <Caps size={Caps.sizes.small} color={Caps.colors.secondary} strong>
          Caps (Small, secondary, strong)
        </Caps>
      </div>
      <div>
        <Caps size={Caps.sizes.small} color={Caps.colors.secondary}>
          Caps (Small, secondary)
        </Caps>
      </div>
    </React.Fragment>
  )
}

export default Example
```

## Usage & types

<Usage
  install="npm install @pluralsight/ps-design-system-text"
  import="import { Caps } from '@pluralsight/ps-design-system-text'"
  packageName="text"
  version={props.version}
/>

<TypesTable>
  <TypesProp
    name="size"
    type={<TypesEnum enum={Caps.sizes} />}
    desc="size (from Caps.sizes)"
    default={Label.sizes.normal}
  />
  <TypesProp
    name="color"
    type={<TypesEnum enum={Caps.colors} />}
    desc="color (from Caps.colors)"
    default={Caps.colors.normal}
  />
  <TypesProp name="strong" type="boolean" desc="Bold style" default="false" />
</TypesTable>
